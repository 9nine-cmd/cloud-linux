路径和目录文件的创建
  【Linux学习(2)----路径和目录文件的创建】 https://www.bilibili.com/video/BV1nC4y1P7U8/?share_source=copy_web&vd_source=daa6acab11bda9bb108ac672d6431d20
  pwd— 显示当前工作目录
  cd— 切换目录
    /根目录
    ~当前用户家目录
    .当前目录
    ..上级目录
    -上一次所在目录
  ls— 列出目录内容
    -l详细信息（权限、大小、时间）
    -a 包含隐藏文件（以 . 开头）
    -la组合使用
  # ls加文件路径可以直接不进入文件夹看有什么文件
    /etc指定路径
  mkdir— 创建目录
    -p自动创建父目录
    -m创建时指定权限（如 -m 755）
  touch— 创建空文件 / 更新时间戳（当文件存在不能覆盖内容）
    touch file.txt              # 创建空文件（不存在则创建）
    touch a.txt b.txt           # 创建多个文件
    touch -t 202501011200 file.txt   # 设置指定时间戳
  cp file.txt copy.txt              # 复制文件
  cp -r dir1 dir2                   # 递归复制目录
  cp file.txt /tmp/                 # 复制到指定目录
    -r递归复制目录
    -i覆盖前询问
    -v显示复制过程
  mv— 移动 / 重命名
    mv old.txt new.txt                # 重命名
    mv ./目录位置（可省略）/file.txt /tmp/                 # 移动到其他目录
    mv dir1 /opt/dir1                 # 移动目录
  ln— 创建链接
    ln file.txt hardlink.txt          # 创建硬链接
    ln -s file.txt softlink.txt      # 创建软链接（符号链接）
  rm file.txt             # 删除文件(依旧填充路径可以不进入文件直接删）
    rm -r dir/              # 递归删除目录
    rm -rf dir/             # 强制递归删除（⚠️ 慎用！）
    rm file #存在就删，不存在报错
    rm -i file #删除前问你 y/n
    rm -f file #不问，直接删，没文件也不吭声
查看文件
  cat note.txt  #直接显示文件内容
  cat head -n x（显示多少行） note.txt
  cat tail -n x note .txt
  tail -f加文件路径（实时监控配合echo使用可以实时监控到echo添加进入的内容）
echo "hello world" > file.txt          # 覆盖
echo "second line" >> file.txt         # 追加
cat > file.txt << EOF                  # 多行写入
line1
line2
EOF
# 输入内容后按 Ctrl+D 保存
  echo "test" | tee output.txt        # 创建并写入（同时输出到屏幕）
  echo "test" | tee -a output.txt     # 追加写入
 文本编辑器创建文件
  vim file.txt        # Vim
  nano file.txt       # Nano
插入模式：开始写内容
  i光标前插入
  a光标后插入
  o当前行下方新开一行插入
  O当前行上方新开一行插入
  I行首插入
  A行尾插入
普通模式下按 :进入命令模式：
  :w	保存
  :q	退出
  :wq	保存并退出
  :q!	强制退出（不保存）
  :wq!	强制保存退出
  :x	同 :wq
  ZZ	普通模式下直接 ZZ= 保存退出（不用输冒号）
查找
  /hello          # 向下查找 hello，n 下一个，N 上一个
  ?hello          # 向上查找
替换
  :%s/old/new/g       # 全局替换 old → new
  :%s/old/new/gc      # 替换前逐个确认
权限速查小抄（可直接复制粘贴）
  列出 sudo 权限：sudo -l
  更新并升级系统：sudo apt update && sudo apt upgrade
  创建目录并改所有者：sudo mkdir -p /home/yourname/workspace && sudo chown yourname:yourname /home/yourname/workspace
  编辑 /etc/sudoers：sudo visudo
  以用户 alice 执行命令：sudo -u alice command
